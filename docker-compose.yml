version: "3.9"

services:
  api:
    build: .
    container_name: lectureai_api
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - uploads_data:/app/uploads
    env_file:
      - .env
    restart: unless-stopped

volumes:
  uploads_data:
